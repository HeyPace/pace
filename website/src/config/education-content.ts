export interface EditorialContent {
  title: string;
  description: string;
  path: string;
  intent: string;
  schema: string[];
  internalLinks: string[];
  markdown: string;
  html: string;
}

export const educationContent: Record<string, EditorialContent> = {
  "/on-device-ai-assistant-mac": {
    "title": "On-device AI assistant for Mac",
    "description": "Learn what an on-device AI assistant for Mac should keep local, how permissions and optional cloud paths work, and how Pace connects voice, screen context, actions, and memory.",
    "path": "/on-device-ai-assistant-mac",
    "intent": "category explanation for people seeking a local AI assistant on macOS",
    "schema": [
      "Article",
      "SoftwareApplication",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/screen-aware-ai-assistant-mac",
      "/private-voice-assistant-mac",
      "/mac-ai-assistant-actions",
      "/on-device-meeting-notes-mac",
      "/privacy",
      "/download",
      "/faq"
    ],
    "markdown": "An on-device AI assistant for Mac processes its core interaction loop on the computer: voice capture, transcription, planning, screen context, actions, memory, and speech. The point is not merely that a model can run locally. The point is that useful assistance can happen without making a remote service the default owner of your microphone, screen, and work history.\n\n## What should stay on the Mac\n\nA credible local mode keeps raw voice, screenshots or accessibility context, conversation state, meeting recordings, and personal journals on the device. Network access should be an explicit feature boundary, not an invisible fallback. The product should show when a request leaves the Mac, which provider receives it, and what local alternative remains.\n\n## What makes an assistant useful\n\nA Mac assistant needs more than chat. It should understand the active application, identify relevant controls, propose an action, request approval for material changes, verify what happened, and remember enough context to help later. It also needs restraint: no silent destructive actions, no surprise background network calls, and clear permissions.\n\n## Pace's model\n\nPace is an Apple Silicon macOS voice and action assistant. In local mode, its planner, screen understanding, speech, retrieval, journals, and meeting synthesis run on the Mac. It can use Apple-native or user-enabled external planner tiers, but off-device paths are explicit, consent-gated, visibly indicated, and recorded in a local privacy audit log. Unattended scheduled work does not silently escalate to the network.\n\nPace can read screen context, execute supported actions through its approval and verification pipeline, teach and run local skills, keep bounded local memory, and capture two-track meeting audio for local transcription and note generation. Individual capabilities have their own permissions and opt-ins; “on-device” does not remove the need for macOS microphone, accessibility, screen, calendar, or other relevant access.\n\n## Who this is for\n\nPace fits people who want a Mac-native assistant with voice, screen context, actions, and memory while keeping local processing as the default. It is not a cross-platform cloud chatbot and does not support Intel Macs.\n\n**CTA:** Download Pace, inspect its privacy model, or continue to the screen-aware and action guides.",
    "html": "<p>An on-device AI assistant for Mac processes its core interaction loop on the computer: voice capture, transcription, planning, screen context, actions, memory, and speech. The point is not merely that a model can run locally. The point is that useful assistance can happen without making a remote service the default owner of your microphone, screen, and work history.</p>\n<h2>What should stay on the Mac</h2>\n<p>A credible local mode keeps raw voice, screenshots or accessibility context, conversation state, meeting recordings, and personal journals on the device. Network access should be an explicit feature boundary, not an invisible fallback. The product should show when a request leaves the Mac, which provider receives it, and what local alternative remains.</p>\n<h2>What makes an assistant useful</h2>\n<p>A Mac assistant needs more than chat. It should understand the active application, identify relevant controls, propose an action, request approval for material changes, verify what happened, and remember enough context to help later. It also needs restraint: no silent destructive actions, no surprise background network calls, and clear permissions.</p>\n<h2>Pace&#39;s model</h2>\n<p>Pace is an Apple Silicon macOS voice and action assistant. In local mode, its planner, screen understanding, speech, retrieval, journals, and meeting synthesis run on the Mac. It can use Apple-native or user-enabled external planner tiers, but off-device paths are explicit, consent-gated, visibly indicated, and recorded in a local privacy audit log. Unattended scheduled work does not silently escalate to the network.</p>\n<p>Pace can read screen context, execute supported actions through its approval and verification pipeline, teach and run local skills, keep bounded local memory, and capture two-track meeting audio for local transcription and note generation. Individual capabilities have their own permissions and opt-ins; “on-device” does not remove the need for macOS microphone, accessibility, screen, calendar, or other relevant access.</p>\n<h2>Who this is for</h2>\n<p>Pace fits people who want a Mac-native assistant with voice, screen context, actions, and memory while keeping local processing as the default. It is not a cross-platform cloud chatbot and does not support Intel Macs.</p>\n<p><strong>CTA:</strong> Download Pace, inspect its privacy model, or continue to the screen-aware and action guides.</p>\n"
  },
  "/screen-aware-ai-assistant-mac": {
    "title": "Screen-aware AI assistant for Mac",
    "description": "Understand how a screen-aware Mac assistant combines accessibility, local vision, interaction state, approval, and post-action verification.",
    "path": "/screen-aware-ai-assistant-mac",
    "intent": "problem and workflow page for people looking for a Mac assistant that understands the screen",
    "schema": [
      "TechArticle",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/on-device-ai-assistant-mac",
      "/mac-ai-assistant-actions",
      "/private-voice-assistant-mac",
      "/privacy",
      "/download"
    ],
    "markdown": "A screen-aware AI assistant uses the state of the active Mac application to answer questions, guide the user, or perform an approved action. It should not treat a screenshot as unlimited authority. Reliable screen assistance combines the least invasive observation available with accessibility structure, visual grounding when necessary, and verification after each action.\n\n## Three kinds of screen context\n\n1. **Accessibility structure:** roles, labels, values, focus, and actions exposed by macOS. This is usually the best source for semantic controls.\n2. **Visual context:** pixels, OCR, or detected marks for applications where accessibility information is incomplete.\n3. **Interaction state:** the active window, recent user input, and the visible change after an action.\n\nEach source has gaps. Accessibility can be incomplete, visual models can misread coordinates, and a visible change may not prove the intended result. A robust assistant cross-checks them and fails visibly when confidence is insufficient.\n\n## The action loop\n\nA useful loop is observe, plan, request approval when required, act, and verify. For a click, verification might confirm a changed accessibility value or a new window state. For a workflow guide, the assistant should advance only after the user's click matches the expected state. A coordinate alone is not a durable outcome.\n\n## Pace's screen model\n\nPace can use local accessibility context, OCR, and a local vision model. Its click executor scores candidates, verifies the result, and has recovery paths for ambiguous targets. Watch mode records bounded local observations when enabled. Pace can also draw guidance on screen and wait for the user to complete the expected step. Screen and accessibility permissions remain controlled through macOS and Pace's own settings.\n\n## Privacy and limits\n\nIn local mode, screen analysis stays on the Mac. If a user selects an off-device planner tier, Pace shows the external state and records it in the local audit log; capability-specific privacy pins can still require local processing. Users should disable screen or journal sources they do not want and clear local history through settings.\n\n**CTA:** See how approved Mac actions work, review privacy controls, or download Pace.",
    "html": "<p>A screen-aware AI assistant uses the state of the active Mac application to answer questions, guide the user, or perform an approved action. It should not treat a screenshot as unlimited authority. Reliable screen assistance combines the least invasive observation available with accessibility structure, visual grounding when necessary, and verification after each action.</p>\n<h2>Three kinds of screen context</h2>\n<ol>\n<li><strong>Accessibility structure:</strong> roles, labels, values, focus, and actions exposed by macOS. This is usually the best source for semantic controls.</li>\n<li><strong>Visual context:</strong> pixels, OCR, or detected marks for applications where accessibility information is incomplete.</li>\n<li><strong>Interaction state:</strong> the active window, recent user input, and the visible change after an action.</li>\n</ol>\n<p>Each source has gaps. Accessibility can be incomplete, visual models can misread coordinates, and a visible change may not prove the intended result. A robust assistant cross-checks them and fails visibly when confidence is insufficient.</p>\n<h2>The action loop</h2>\n<p>A useful loop is observe, plan, request approval when required, act, and verify. For a click, verification might confirm a changed accessibility value or a new window state. For a workflow guide, the assistant should advance only after the user&#39;s click matches the expected state. A coordinate alone is not a durable outcome.</p>\n<h2>Pace&#39;s screen model</h2>\n<p>Pace can use local accessibility context, OCR, and a local vision model. Its click executor scores candidates, verifies the result, and has recovery paths for ambiguous targets. Watch mode records bounded local observations when enabled. Pace can also draw guidance on screen and wait for the user to complete the expected step. Screen and accessibility permissions remain controlled through macOS and Pace&#39;s own settings.</p>\n<h2>Privacy and limits</h2>\n<p>In local mode, screen analysis stays on the Mac. If a user selects an off-device planner tier, Pace shows the external state and records it in the local audit log; capability-specific privacy pins can still require local processing. Users should disable screen or journal sources they do not want and clear local history through settings.</p>\n<p><strong>CTA:</strong> See how approved Mac actions work, review privacy controls, or download Pace.</p>\n"
  },
  "/private-voice-assistant-mac": {
    "title": "Private voice assistant for Mac",
    "description": "Evaluate a private Mac voice assistant by its audio, planning, storage, permissions, and network boundaries—and see how Pace exposes local and optional external paths.",
    "path": "/private-voice-assistant-mac",
    "intent": "privacy-focused category page for Mac voice assistant buyers",
    "schema": [
      "Article",
      "FAQPage",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/on-device-ai-assistant-mac",
      "/screen-aware-ai-assistant-mac",
      "/on-device-meeting-notes-mac",
      "/privacy",
      "/faq",
      "/download"
    ],
    "markdown": "A private voice assistant should make its data path understandable. “Private” is not just a model label: it depends on where audio is transcribed, where prompts are planned, where speech is generated, what is stored, which permissions are enabled, and whether a network fallback can occur.\n\n## Ask five questions\n\n1. Where does raw microphone audio go?\n2. Is transcription local, external, or configurable?\n3. Can screen context or conversation history leave the device?\n4. What is stored, for how long, and how can it be cleared?\n5. Does the interface clearly indicate an off-device request before it happens?\n\n## Pace's local path\n\nPace supports local speech recognition, a local planner, local vision, local text-to-speech, and local retrieval on Apple Silicon. Conversation memory, research history, screen-watch observations, app-usage journals, taught skills, and meeting artifacts are stored on the Mac under bounded policies described in the app and documentation. Users can enable, inspect, and clear relevant sources.\n\n## Optional external paths\n\nPace can also use Apple Foundation Models, installed Codex or Claude command-line tools, or user-configured API providers. Those paths are optional and do not become invisible fallbacks. External planner choices require consent, show an off-device indicator, and write a local audit record. Scheduled background work stays local unless its separate consent and soak conditions allow the configured external route. Meeting-note synthesis is privacy-pinned to local processing even when another planner tier is enabled elsewhere.\n\n## Permissions still matter\n\nA local application may need microphone, accessibility, screen recording, contacts, calendar, reminders, or other macOS permissions to perform a requested task. Local processing reduces data transfer; it does not remove the need to grant and review access. Use the minimum set for the capabilities you want.\n\n## Honest boundary\n\nNo local assistant can promise that every optional integration is network-free. Downloading models, fetching a user-requested URL, calling an external MCP server, or choosing a cloud planner uses the network. The useful privacy promise is that the boundary is explicit, controllable, and auditable.\n\n**CTA:** Read Pace's privacy policy and FAQ, then download the app for local mode.",
    "html": "<p>A private voice assistant should make its data path understandable. “Private” is not just a model label: it depends on where audio is transcribed, where prompts are planned, where speech is generated, what is stored, which permissions are enabled, and whether a network fallback can occur.</p>\n<h2>Ask five questions</h2>\n<ol>\n<li>Where does raw microphone audio go?</li>\n<li>Is transcription local, external, or configurable?</li>\n<li>Can screen context or conversation history leave the device?</li>\n<li>What is stored, for how long, and how can it be cleared?</li>\n<li>Does the interface clearly indicate an off-device request before it happens?</li>\n</ol>\n<h2>Pace&#39;s local path</h2>\n<p>Pace supports local speech recognition, a local planner, local vision, local text-to-speech, and local retrieval on Apple Silicon. Conversation memory, research history, screen-watch observations, app-usage journals, taught skills, and meeting artifacts are stored on the Mac under bounded policies described in the app and documentation. Users can enable, inspect, and clear relevant sources.</p>\n<h2>Optional external paths</h2>\n<p>Pace can also use Apple Foundation Models, installed Codex or Claude command-line tools, or user-configured API providers. Those paths are optional and do not become invisible fallbacks. External planner choices require consent, show an off-device indicator, and write a local audit record. Scheduled background work stays local unless its separate consent and soak conditions allow the configured external route. Meeting-note synthesis is privacy-pinned to local processing even when another planner tier is enabled elsewhere.</p>\n<h2>Permissions still matter</h2>\n<p>A local application may need microphone, accessibility, screen recording, contacts, calendar, reminders, or other macOS permissions to perform a requested task. Local processing reduces data transfer; it does not remove the need to grant and review access. Use the minimum set for the capabilities you want.</p>\n<h2>Honest boundary</h2>\n<p>No local assistant can promise that every optional integration is network-free. Downloading models, fetching a user-requested URL, calling an external MCP server, or choosing a cloud planner uses the network. The useful privacy promise is that the boundary is explicit, controllable, and auditable.</p>\n<p><strong>CTA:</strong> Read Pace&#39;s privacy policy and FAQ, then download the app for local mode.</p>\n"
  },
  "/mac-ai-assistant-actions": {
    "title": "Mac AI assistant that can take actions",
    "description": "See how a Mac AI assistant can take approved, typed, verifiable actions through accessibility, app integrations, Shortcuts, tools, and recoverable workflows.",
    "path": "/mac-ai-assistant-actions",
    "intent": "workflow and category page for people seeking an action-taking Mac assistant",
    "schema": [
      "HowTo",
      "TechArticle",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/on-device-ai-assistant-mac",
      "/screen-aware-ai-assistant-mac",
      "/private-voice-assistant-mac",
      "/faq",
      "/download"
    ],
    "markdown": "A Mac AI assistant that takes actions should do more than generate instructions, but less than silently control the computer. The safe product boundary is an observable action loop: understand the request, inspect current state, propose typed steps, ask for approval when risk requires it, execute through the strongest available interface, verify the result, and preserve an undo or audit trail where possible.\n\n## Prefer semantic actions\n\nUse application APIs, App Intents, Shortcuts, MCP tools, or the macOS Accessibility API when they expose the requested operation. These interfaces carry labels and state that are more durable than raw screen coordinates. Use visual grounding and pointer events as a fallback for applications without sufficient semantic access.\n\n## Separate planning from authority\n\nThe model can propose an action, but policy should decide whether it may run. Read-only navigation, reversible edits, external messages, downloads, and destructive mutations need different treatment. A planner output should be parsed into typed actions and validated before execution.\n\n## Verify after every material step\n\nA click event only proves that input was sent. Verification should inspect the resulting window, control value, file, reminder, message draft, or other expected state. If the result is ambiguous, stop or recover; do not continue a long workflow on an assumed success.\n\n## Pace's action stack\n\nPace includes a typed tool registry, a plan-act-observe loop, application integrations, App Intents and deeplinks, accessibility and pointer execution, click scoring and recovery, undo for supported reversible mutations, failure narration, and local action history. Users can also record literal flows, install recipes, or teach natural-language skills that are re-grounded against the current screen. MCP connections and external services are optional.\n\n## Good first workflows\n\nStart with bounded tasks whose outcome can be checked: open an application, create a reminder, draft rather than send an email, navigate a known interface, record a repeatable flow, or guide the user through an unfamiliar screen. Expand authority only after the verification path is reliable.\n\n**CTA:** Review Pace's action and privacy model, then download it for a supported Mac.",
    "html": "<p>A Mac AI assistant that takes actions should do more than generate instructions, but less than silently control the computer. The safe product boundary is an observable action loop: understand the request, inspect current state, propose typed steps, ask for approval when risk requires it, execute through the strongest available interface, verify the result, and preserve an undo or audit trail where possible.</p>\n<h2>Prefer semantic actions</h2>\n<p>Use application APIs, App Intents, Shortcuts, MCP tools, or the macOS Accessibility API when they expose the requested operation. These interfaces carry labels and state that are more durable than raw screen coordinates. Use visual grounding and pointer events as a fallback for applications without sufficient semantic access.</p>\n<h2>Separate planning from authority</h2>\n<p>The model can propose an action, but policy should decide whether it may run. Read-only navigation, reversible edits, external messages, downloads, and destructive mutations need different treatment. A planner output should be parsed into typed actions and validated before execution.</p>\n<h2>Verify after every material step</h2>\n<p>A click event only proves that input was sent. Verification should inspect the resulting window, control value, file, reminder, message draft, or other expected state. If the result is ambiguous, stop or recover; do not continue a long workflow on an assumed success.</p>\n<h2>Pace&#39;s action stack</h2>\n<p>Pace includes a typed tool registry, a plan-act-observe loop, application integrations, App Intents and deeplinks, accessibility and pointer execution, click scoring and recovery, undo for supported reversible mutations, failure narration, and local action history. Users can also record literal flows, install recipes, or teach natural-language skills that are re-grounded against the current screen. MCP connections and external services are optional.</p>\n<h2>Good first workflows</h2>\n<p>Start with bounded tasks whose outcome can be checked: open an application, create a reminder, draft rather than send an email, navigate a known interface, record a repeatable flow, or guide the user through an unfamiliar screen. Expand authority only after the verification path is reliable.</p>\n<p><strong>CTA:</strong> Review Pace&#39;s action and privacy model, then download it for a supported Mac.</p>\n"
  },
  "/on-device-meeting-notes-mac": {
    "title": "On-device meeting notes for Mac",
    "description": "Learn how on-device meeting notes capture two audio tracks, transcribe and summarize locally, ground action items, and keep retention under user control.",
    "path": "/on-device-meeting-notes-mac",
    "intent": "use-case page for people seeking private local meeting notes on Mac",
    "schema": [
      "HowTo",
      "FAQPage",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/on-device-ai-assistant-mac",
      "/private-voice-assistant-mac",
      "/privacy",
      "/faq",
      "/download"
    ],
    "markdown": "On-device meeting notes keep capture, transcription, and synthesis on the Mac instead of uploading a conversation to a hosted note-taking service. This matters most when the meeting contains customer data, hiring discussions, strategy, credentials, or other information that should not become a third party's default input.\n\n## The local pipeline\n\nA complete workflow captures the user's microphone and the other side of the call, preserves the tracks, transcribes them locally, segments the conversation, and generates a summary, decisions, and action items with links back to source moments. Storage location, retention, and deletion should be visible.\n\n## Pace meeting mode\n\nPace records two local audio tracks for a meeting: the user's microphone and system or call audio. When the meeting stops, local transcription feeds a privacy-pinned local note model. The output includes structured notes and transcript-grounded action items. Meeting files are ordinary local artifacts under Pace's application-support directory and use a configurable retention policy documented in the product; the current default described by the public FAQ is 30 days.\n\nThe privacy pin is important: enabling a cloud or command-line planner elsewhere in Pace does not allow meeting-note synthesis to use it. A user can still choose external applications or workflows outside that boundary, but Pace's meeting synthesis path remains local.\n\n## What to evaluate\n\n- Does the app capture both sides reliably on the target Mac?\n- Are transcripts and notes generated without a network fallback?\n- Can an action item show the source quote or timestamp?\n- Are files easy to locate, export, and delete?\n- Is retention configurable and enforced?\n- Are microphone and screen or system-audio permissions clear?\n\n## Honest limits\n\nLocal transcription and synthesis consume memory, storage, and battery. Accuracy depends on audio quality, speakers, language, and the installed local models. A generated summary should be treated as a draft, and important decisions should be checked against the transcript.\n\n**CTA:** Review Pace's meeting privacy details and FAQ, then download Pace for Apple Silicon.",
    "html": "<p>On-device meeting notes keep capture, transcription, and synthesis on the Mac instead of uploading a conversation to a hosted note-taking service. This matters most when the meeting contains customer data, hiring discussions, strategy, credentials, or other information that should not become a third party&#39;s default input.</p>\n<h2>The local pipeline</h2>\n<p>A complete workflow captures the user&#39;s microphone and the other side of the call, preserves the tracks, transcribes them locally, segments the conversation, and generates a summary, decisions, and action items with links back to source moments. Storage location, retention, and deletion should be visible.</p>\n<h2>Pace meeting mode</h2>\n<p>Pace records two local audio tracks for a meeting: the user&#39;s microphone and system or call audio. When the meeting stops, local transcription feeds a privacy-pinned local note model. The output includes structured notes and transcript-grounded action items. Meeting files are ordinary local artifacts under Pace&#39;s application-support directory and use a configurable retention policy documented in the product; the current default described by the public FAQ is 30 days.</p>\n<p>The privacy pin is important: enabling a cloud or command-line planner elsewhere in Pace does not allow meeting-note synthesis to use it. A user can still choose external applications or workflows outside that boundary, but Pace&#39;s meeting synthesis path remains local.</p>\n<h2>What to evaluate</h2>\n<ul>\n<li>Does the app capture both sides reliably on the target Mac?</li>\n<li>Are transcripts and notes generated without a network fallback?</li>\n<li>Can an action item show the source quote or timestamp?</li>\n<li>Are files easy to locate, export, and delete?</li>\n<li>Is retention configurable and enforced?</li>\n<li>Are microphone and screen or system-audio permissions clear?</li>\n</ul>\n<h2>Honest limits</h2>\n<p>Local transcription and synthesis consume memory, storage, and battery. Accuracy depends on audio quality, speakers, language, and the installed local models. A generated summary should be treated as a draft, and important decisions should be checked against the transcript.</p>\n<p><strong>CTA:</strong> Review Pace&#39;s meeting privacy details and FAQ, then download Pace for Apple Silicon.</p>\n"
  },
  "/on-device-mac-voice-control-privacy": {
    "title": "How On-Device Mac Voice Assistants Control Desktop Actions Privately",
    "description": "Learn how macOS voice agents process audio, interpret screens, and execute local desktop actions entirely on-device without cloud LLMs or off-site telemetry.",
    "path": "/on-device-mac-voice-control-privacy",
    "intent": "technical explainer for privacy-conscious users evaluating how local Mac voice assistants control desktop actions",
    "schema": [
      "TechArticle",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/private-voice-assistant-mac",
      "/screen-aware-ai-assistant-mac",
      "/mac-ai-assistant-actions",
      "/on-device-ai-assistant-mac",
      "/privacy",
      "/download"
    ],
    "markdown": "Voice interfaces on desktop operating systems present a stark architectural trade-off. Traditional cloud-based AI assistants require continuous streaming of sensitive user context—including live microphone audio, high-resolution display frames, document content, and active application state—to remote cloud infrastructure. While centralized models offer extensive parameter capacity, sending full operating system state across the network introduces significant data security risks. For software developers, legal counsel, healthcare workers, and enterprise professionals handling confidential code or proprietary communications, transmitting unencrypted desktop context to third-party endpoints is often unacceptable.\n\nBuilding a truly private voice assistant requires fundamentally shifting the execution boundary from remote cloud infrastructure back to the local Mac hardware. By running speech recognition, reasoning, context extraction, and action execution locally on Apple Silicon, a native macOS voice agent can automate desktop tasks without a single byte of private user context leaving the machine.\n\n## The Four Layers of an On-Device Voice Control Architecture\n\nTo achieve full voice control over desktop applications while keeping processing local, an architecture must unify four distinct system components:\n\n### 1. On-Device Automatic Speech Recognition (ASR)\n\nVoice control begins with capturing spoken audio and converting it into textual transcripts. Rather than streaming raw PCM audio buffers across the internet, an on-device architecture utilizes native operating system frameworks such as Apple’s `SFSpeechRecognizer` configured strictly with on-device recognition mode (`requiresOnDeviceRecognition = true`). This processes spoken input locally using Neural Engine hardware, delivering low-latency text transcription without external network requests or third-party audio retention.\n\n### 2. Local Multimodal Reasoning and Plan Generation\n\nOnce transcribed, user commands are processed by a local language model or multimodal reasoner (such as `qwen/qwen3.5-4b` running via a local loopback server or an embedded MLX runtime). The local reasoner analyzes the user's request alongside current application context. Instead of generating arbitrary unstructured text, the model emits structured action structures or precise tool calls—such as opening an application, clicking a specific visual element, typing text into an active control, or executing a hotkey chord.\n\n### 3. On-Device Text-to-Speech (TTS)\n\nTo maintain natural conversational interaction, spoken audio output must also be synthesized locally. A native implementation leverages built-in system synthesizers like `AVSpeechSynthesizer` or lightweight local inference sidecars (such as Kokoro-82M serving audio streams over local loopback interfaces). Synthesizing speech directly on the host machine avoids sending text responses back to cloud audio endpoints.\n\n### 4. Native macOS Accessibility Execution Layer\n\nExecuting desktop actions requires interacting directly with the macOS window server and Accessibility hierarchy. The agent parses generated action commands and maps them to native `AXUIElement` Accessibility API calls (such as `AXUIElementPerformAction` for button presses or value mutations). When accessibility targets are ambiguous or unexposed, the system can fallback to direct system events (`CGEvent`) for precise cursor movement and clicks, allowing the agent to control native desktop applications predictably.\n\n## macOS System Permissions and the Local Trust Boundary\n\nAn on-device voice control system operates strictly within the security boundaries enforced by macOS Transparency, Consent, and Control (TCC). Every capability required for automated desktop interaction maps to an explicit user permission:\n\n- **Microphone Access**: Grants audio capture rights exclusively to the local application process during push-to-talk or active listening modes.\n- **Screen Recording Permission**: Enables the application to capture local screen contents using frameworks such as `ScreenCaptureKit` for visual context extraction.\n- **Accessibility Permission**: Authorizes the process to inspect window element trees, query control labels, and perform action synthesis across running applications.\n\nBecause processing occurs locally, all internal communication between UI components, local model processes, and sidecar utilities is restricted to loopback addresses (`127.0.0.1` / `localhost`) or local Unix domain sockets. External network ports remain unexposed.\n\nData persistence is similarly contained. Thread memory summaries, episodic facts, app usage journals, and research histories are stored as local files (e.g., JSON structures stored in the user's `~/Library/Application Support/` directory). No background analytics SDKs or remote telemetries track user speech patterns, typed inputs, or active application titles.\n\n## Action Safety, User Approval, and Reversibility\n\nExecuting desktop actions on behalf of a user introduces operational risk—such as inadvertently closing an unsaved document, modifying system preferences, or sending an incomplete draft. A robust on-device voice agent mitigates these risks through a clear action safety model:\n\n1. **Risk Classification**: Actions are categorized by risk profile. Routine, non-destructive read operations (such as opening a local app, reading a window title, or copying text to the system clipboard) proceed automatically for fluid interaction.\n2. **Explicit Action Approval**: Higher-risk or non-undoable mutations (such as deleting files, modifying calendar entries, or sending outbound messages) trigger an explicit approval popup. This prompt halts execution until the user explicitly confirms or cancels the action.\n3. **Visual Confirmation and Local Undo**: When a reversible state mutation executes (such as creating a local note or editing active text), the interface displays a visual status indicator and a brief, interactive undo banner. Tapping undo or issuing a spoken \"undo that\" command triggers a reverse operation, restoring the previous local state from a session-level mutation log.\n\n## Honest Architectural Exceptions: When Work Crosses the On-Device Boundary\n\nWhile an on-device architecture prioritizes strict local isolation as its operational default, user requirements may occasionally demand external model capabilities or remote network tools. A privacy-first application handles these exceptions transparently rather than obfuscating network calls:\n\n- **Bring-Your-Own-Key (BYO-Key) Direct APIs & CLI Bridges**: Users may explicitly configure optional cloud planner tiers (such as direct API keys or local CLI bridges to external reasoning tools like `codex` or `claude`).\n- **Explicit Consent and Guardrails**: Off-device routing requires explicit user enablement. In cases where external CLI tools are integrated, additional safety gates—such as explicit transport consent and 24-hour soak periods—ensure background tasks never quietly route data off-device.\n- **Visual Signals and Audit Logging**: Whenever an active request uses an off-device path, the system interface changes its visual state (e.g., tinting the active menu bar capsule amber). Simultaneously, the application writes an entry to a local API audit log (`~/Library/Application Support/Pace/api-audit-log.jsonl`), recording the destination and payload byte size so users can verify all external activity.\n- **Approval-Gated Network Tools**: Built-in tools that fetch external web resources (such as downloading a user-requested file from an HTTP/HTTPS URL to `~/Downloads`) validate input parameters, require explicit approval before dispatch, and perform no background data transmission beyond the explicit request.\n\n## Conclusion and Key Architectural Takeaways\n\nOn-device voice control demonstrates that powerful desktop automation does not require surrendering personal data to cloud servers. By combining native macOS speech recognition, on-device multimodal models, native Accessibility framework integration, explicit TCC permissions, and transparent action safety controls, Mac users can automate complex workflows with complete confidence in their privacy.\n\n## Clear Next Action\n\nTo explore how an on-device voice assistant operates on your Mac without streaming audio or screen data off-device, review the complete architecture details on our [privacy documentation](/privacy) or test local push-to-talk voice control by [downloading Pace for macOS](/download).",
    "html": "<p>Voice interfaces on desktop operating systems present a stark architectural trade-off. Traditional cloud-based AI assistants require continuous streaming of sensitive user context—including live microphone audio, high-resolution display frames, document content, and active application state—to remote cloud infrastructure. While centralized models offer extensive parameter capacity, sending full operating system state across the network introduces significant data security risks. For software developers, legal counsel, healthcare workers, and enterprise professionals handling confidential code or proprietary communications, transmitting unencrypted desktop context to third-party endpoints is often unacceptable.</p>\n<p>Building a truly private voice assistant requires fundamentally shifting the execution boundary from remote cloud infrastructure back to the local Mac hardware. By running speech recognition, reasoning, context extraction, and action execution locally on Apple Silicon, a native macOS voice agent can automate desktop tasks without a single byte of private user context leaving the machine.</p>\n<h2>The Four Layers of an On-Device Voice Control Architecture</h2>\n<p>To achieve full voice control over desktop applications while keeping processing local, an architecture must unify four distinct system components:</p>\n<h3>1. On-Device Automatic Speech Recognition (ASR)</h3>\n<p>Voice control begins with capturing spoken audio and converting it into textual transcripts. Rather than streaming raw PCM audio buffers across the internet, an on-device architecture utilizes native operating system frameworks such as Apple’s <code>SFSpeechRecognizer</code> configured strictly with on-device recognition mode (<code>requiresOnDeviceRecognition = true</code>). This processes spoken input locally using Neural Engine hardware, delivering low-latency text transcription without external network requests or third-party audio retention.</p>\n<h3>2. Local Multimodal Reasoning and Plan Generation</h3>\n<p>Once transcribed, user commands are processed by a local language model or multimodal reasoner (such as <code>qwen/qwen3.5-4b</code> running via a local loopback server or an embedded MLX runtime). The local reasoner analyzes the user&#39;s request alongside current application context. Instead of generating arbitrary unstructured text, the model emits structured action structures or precise tool calls—such as opening an application, clicking a specific visual element, typing text into an active control, or executing a hotkey chord.</p>\n<h3>3. On-Device Text-to-Speech (TTS)</h3>\n<p>To maintain natural conversational interaction, spoken audio output must also be synthesized locally. A native implementation leverages built-in system synthesizers like <code>AVSpeechSynthesizer</code> or lightweight local inference sidecars (such as Kokoro-82M serving audio streams over local loopback interfaces). Synthesizing speech directly on the host machine avoids sending text responses back to cloud audio endpoints.</p>\n<h3>4. Native macOS Accessibility Execution Layer</h3>\n<p>Executing desktop actions requires interacting directly with the macOS window server and Accessibility hierarchy. The agent parses generated action commands and maps them to native <code>AXUIElement</code> Accessibility API calls (such as <code>AXUIElementPerformAction</code> for button presses or value mutations). When accessibility targets are ambiguous or unexposed, the system can fallback to direct system events (<code>CGEvent</code>) for precise cursor movement and clicks, allowing the agent to control native desktop applications predictably.</p>\n<h2>macOS System Permissions and the Local Trust Boundary</h2>\n<p>An on-device voice control system operates strictly within the security boundaries enforced by macOS Transparency, Consent, and Control (TCC). Every capability required for automated desktop interaction maps to an explicit user permission:</p>\n<ul>\n<li><strong>Microphone Access</strong>: Grants audio capture rights exclusively to the local application process during push-to-talk or active listening modes.</li>\n<li><strong>Screen Recording Permission</strong>: Enables the application to capture local screen contents using frameworks such as <code>ScreenCaptureKit</code> for visual context extraction.</li>\n<li><strong>Accessibility Permission</strong>: Authorizes the process to inspect window element trees, query control labels, and perform action synthesis across running applications.</li>\n</ul>\n<p>Because processing occurs locally, all internal communication between UI components, local model processes, and sidecar utilities is restricted to loopback addresses (<code>127.0.0.1</code> / <code>localhost</code>) or local Unix domain sockets. External network ports remain unexposed.</p>\n<p>Data persistence is similarly contained. Thread memory summaries, episodic facts, app usage journals, and research histories are stored as local files (e.g., JSON structures stored in the user&#39;s <code>~/Library/Application Support/</code> directory). No background analytics SDKs or remote telemetries track user speech patterns, typed inputs, or active application titles.</p>\n<h2>Action Safety, User Approval, and Reversibility</h2>\n<p>Executing desktop actions on behalf of a user introduces operational risk—such as inadvertently closing an unsaved document, modifying system preferences, or sending an incomplete draft. A robust on-device voice agent mitigates these risks through a clear action safety model:</p>\n<ol>\n<li><strong>Risk Classification</strong>: Actions are categorized by risk profile. Routine, non-destructive read operations (such as opening a local app, reading a window title, or copying text to the system clipboard) proceed automatically for fluid interaction.</li>\n<li><strong>Explicit Action Approval</strong>: Higher-risk or non-undoable mutations (such as deleting files, modifying calendar entries, or sending outbound messages) trigger an explicit approval popup. This prompt halts execution until the user explicitly confirms or cancels the action.</li>\n<li><strong>Visual Confirmation and Local Undo</strong>: When a reversible state mutation executes (such as creating a local note or editing active text), the interface displays a visual status indicator and a brief, interactive undo banner. Tapping undo or issuing a spoken \"undo that\" command triggers a reverse operation, restoring the previous local state from a session-level mutation log.</li>\n</ol>\n<h2>Honest Architectural Exceptions: When Work Crosses the On-Device Boundary</h2>\n<p>While an on-device architecture prioritizes strict local isolation as its operational default, user requirements may occasionally demand external model capabilities or remote network tools. A privacy-first application handles these exceptions transparently rather than obfuscating network calls:</p>\n<ul>\n<li><strong>Bring-Your-Own-Key (BYO-Key) Direct APIs &amp; CLI Bridges</strong>: Users may explicitly configure optional cloud planner tiers (such as direct API keys or local CLI bridges to external reasoning tools like <code>codex</code> or <code>claude</code>).</li>\n<li><strong>Explicit Consent and Guardrails</strong>: Off-device routing requires explicit user enablement. In cases where external CLI tools are integrated, additional safety gates—such as explicit transport consent and 24-hour soak periods—ensure background tasks never quietly route data off-device.</li>\n<li><strong>Visual Signals and Audit Logging</strong>: Whenever an active request uses an off-device path, the system interface changes its visual state (e.g., tinting the active menu bar capsule amber). Simultaneously, the application writes an entry to a local API audit log (<code>~/Library/Application Support/Pace/api-audit-log.jsonl</code>), recording the destination and payload byte size so users can verify all external activity.</li>\n<li><strong>Approval-Gated Network Tools</strong>: Built-in tools that fetch external web resources (such as downloading a user-requested file from an HTTP/HTTPS URL to <code>~/Downloads</code>) validate input parameters, require explicit approval before dispatch, and perform no background data transmission beyond the explicit request.</li>\n</ul>\n<h2>Conclusion and Key Architectural Takeaways</h2>\n<p>On-device voice control demonstrates that powerful desktop automation does not require surrendering personal data to cloud servers. By combining native macOS speech recognition, on-device multimodal models, native Accessibility framework integration, explicit TCC permissions, and transparent action safety controls, Mac users can automate complex workflows with complete confidence in their privacy.</p>\n<h2>Clear Next Action</h2>\n<p>To explore how an on-device voice assistant operates on your Mac without streaming audio or screen data off-device, review the complete architecture details on our <a href=\"/privacy\">privacy documentation</a> or test local push-to-talk voice control by <a href=\"/download\">downloading Pace for macOS</a>.</p>"
  },
  "/local-vs-cloud-voice-agents-macos-checklist": {
    "title": "Local vs Cloud Voice Agents for macOS: Evaluation Checklist",
    "description": "Evaluate local vs. cloud macOS voice agents across latency, offline capability, screen privacy, key storage, auditability, and action execution.",
    "path": "/local-vs-cloud-voice-agents-macos-checklist",
    "intent": "evaluation checklist for security-minded buyers comparing local and cloud macOS voice agents",
    "schema": [
      "TechArticle",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/on-device-ai-assistant-mac",
      "/on-device-mac-voice-control-privacy",
      "/private-voice-assistant-mac",
      "/compared",
      "/pricing",
      "/privacy",
      "/download"
    ],
    "markdown": "Voice assistants on macOS have evolved beyond simple dictation tools into active software agents capable of reading screen contents, reasoning over complex commands, and performing multi-step desktop actions across native applications. As these assistants gain deeper access to operating system capabilities—including the microphone, display contents, clipboard, and Accessibility controls—the underlying system architecture becomes a critical security consideration.\n\nWhen choosing or designing a macOS voice assistant, the central decision lies between **local on-device processing** and **cloud-based AI services**. This evaluation checklist provides software architects, security teams, and power users with a structured framework for assessing privacy posture, latency characteristics, offline resilience, and action safety across local and cloud voice agent implementations.\n\n## The 6-Point Privacy & Security Evaluation Checklist\n\nEvaluating a macOS voice agent requires scrutinizing six key technical dimensions:\n\n### 1. Audio & Speech-to-Text (ASR) Privacy\n\n- **Cloud Architecture**: Microphone audio buffers are compressed and transmitted across the internet to remote transcription endpoints. Audio clips or transcripts may be retained by the cloud provider for model training or logging.\n- **Local Architecture**: Audio processing uses on-device frameworks (such as Apple’s `SFSpeechRecognizer` with `requiresOnDeviceRecognition = true` or local Whisper models). Audio buffers stay in host memory and never touch an external network interface.\n\n### 2. Screen Context & Frame Retention\n\n- **Cloud Architecture**: High-resolution screen captures or active window pixels are uploaded to remote multimodal LLMs. Visual data containing sensitive emails, financial documents, or source code leaves the physical machine.\n- **Local Architecture**: Screen context is captured locally via `ScreenCaptureKit`, processed via native macOS Accessibility (`AXUIElement`) APIs or native OCR, and optionally analyzed by a local Vision-Language Model (VLM) running on loopback (`127.0.0.1`). Frame buffers are purged immediately after execution.\n\n### 3. Network Independence & Offline Capability\n\n- **Cloud Architecture**: Unusable without an active internet connection. Network congestion, DNS failures, or remote API outages halt dictation and action execution.\n- **Local Architecture**: Fully functional offline. Speech recognition, reasoning, and system action execution run locally on Apple Silicon hardware without requiring network connectivity.\n\n### 4. API Key & Credential Storage\n\n- **Cloud Architecture**: Third-party API keys or OAuth tokens may be stored in plain-text configuration files (`.env` or property lists) or managed remotely by service vendors.\n- **Local Architecture**: Credentials for optional external services reside exclusively in the native macOS Keychain (`PaceKeychainStore`), protected by system-level encryption and sandbox boundaries.\n\n### 5. Auditability & System Telemetry\n\n- **Cloud Architecture**: User interaction metrics, spoken command frequencies, and active app names are typically captured by vendor analytics SDKs and uploaded to remote telemetry dashboards.\n- **Local Architecture**: Zero remote telemetry. All execution logs and API audit records stay on the local filesystem (e.g., `~/Library/Application Support/Pace/api-audit-log.jsonl`), giving the user complete visibility and control over their data history.\n\n### 6. Action Safety & Reversible Execution\n\n- **Cloud Architecture**: Remote planner models dispatch action commands without granular local preflight checks or immediate user rollback mechanisms.\n- **Local Architecture**: High-risk actions (such as file deletion or external communications) require explicit modal approval with default-cancel safety. Reversible mutations present a floating 5-second undo banner and local session recovery.\n\n## Comparing Local vs. Cloud Voice Agent Architectures\n\n| Feature / Dimension | Local On-Device Architecture | Cloud-Based AI Architecture |\n| :--- | :--- | :--- |\n| **Audio Processing** | On-device ASR (`SFSpeechRecognizer` / Whisper) | Remote audio streaming |\n| **Screen Data Route** | Local `ScreenCaptureKit` + local VLM | Cloud frame transmission |\n| **Offline Functionality** | Fully operational offline | Inoperable without internet |\n| **Response Latency** | Direct local execution (no network overhead) | Bound by network round-trip time |\n| **Telemetry & Tracking** | Zero remote telemetry; local JSONL logs | Vendor cloud analytics & logging |\n| **Credential Storage** | Protected in native macOS Keychain | Plain-text config or remote servers |\n| **Action Confirmation** | Modal approval prompts + local undo banner | Opaque remote command dispatch |\n| **Hardware Requirement** | Apple Silicon with adequate unified RAM | Minimal local RAM; relies on remote GPUs |\n\n## Managing Off-Device Exceptions Responsibly\n\nWhile an on-device architecture provides maximum privacy by default, certain workflows may require opting into external cloud LLMs (such as using a personal API key or connecting a developer CLI tool like `codex` or `claude`). A responsible system architecture handles off-device exceptions through clear security mechanics:\n\n1. **Explicit Opt-In and Transport Consent**: Off-device routing is never active by default. Users must deliberately enable cloud options and grant transport consent.\n2. **Soak Period Safety Gates**: Sensitive CLI direct-spawns enforce safety timers (such as a 24-hour soak period) to ensure background tasks do not prematurely route data off-device.\n3. **Unambiguous Visual Signals**: Whenever an active request routes off-device, the application UI clearly signals this transition—such as tinting the active menu bar status capsule **amber**.\n4. **Local Audit Logging**: Every off-device turn records the destination, timestamp, and byte payload size in a local API audit log, ensuring complete transparency.\n5. **Fail-Loud Error Recovery**: If an off-device connection fails, the system presents clear error feedback via plain-language failure narrators rather than silently dropping context or falling back unannounced.\n\n## Decision Matrix: Which Architecture Fits Your Workflow?\n\n### Choose a Local On-Device Assistant If You:\n\n- Handle confidential source code, trade secrets, legal client files, or healthcare data subject to compliance requirements.\n- Require reliable voice control and desktop automation while offline or on restricted corporate networks.\n- Want zero telemetry tracking, local Keychain credential security, and total ownership over application logs.\n- Operate modern Apple Silicon hardware with unified RAM configured for local model execution.\n\n### Consider Cloud Planners As Explicit Options If You:\n\n- Require extreme parameter-count reasoning models for complex open-domain queries that exceed local RAM capacity.\n- Work on hardware with limited local memory where heavy local LLMs/VLMs cannot run concurrently.\n- Explicitly consent to remote API routing and maintain appropriate organizational API keys in your macOS Keychain.\n\n## Conclusion and Next Steps\n\nEvaluating desktop voice agents requires looking beyond surface-level features to examine the underlying trust boundary. While cloud architectures offer convenience, local on-device voice agents provide uncompromised data privacy, offline resilience, and transparent action control. By applying this 6-point evaluation checklist, organizations and individual power users can select a voice assistant architecture that aligns with their security requirements.\n\n## Clear Next Action\n\nTo test an on-device voice agent that strictly adheres to this privacy and capability checklist, [download Pace for macOS](/download) or explore our [privacy and trust documentation](/privacy).",
    "html": "<p>Voice assistants on macOS have evolved beyond simple dictation tools into active software agents capable of reading screen contents, reasoning over complex commands, and performing multi-step desktop actions across native applications. As these assistants gain deeper access to operating system capabilities—including the microphone, display contents, clipboard, and Accessibility controls—the underlying system architecture becomes a critical security consideration.</p>\n<p>When choosing or designing a macOS voice assistant, the central decision lies between <strong>local on-device processing</strong> and <strong>cloud-based AI services</strong>. This evaluation checklist provides software architects, security teams, and power users with a structured framework for assessing privacy posture, latency characteristics, offline resilience, and action safety across local and cloud voice agent implementations.</p>\n<h2>The 6-Point Privacy &amp; Security Evaluation Checklist</h2>\n<p>Evaluating a macOS voice agent requires scrutinizing six key technical dimensions:</p>\n<h3>1. Audio &amp; Speech-to-Text (ASR) Privacy</h3>\n<ul>\n<li><strong>Cloud Architecture</strong>: Microphone audio buffers are compressed and transmitted across the internet to remote transcription endpoints. Audio clips or transcripts may be retained by the cloud provider for model training or logging.</li>\n<li><strong>Local Architecture</strong>: Audio processing uses on-device frameworks (such as Apple’s <code>SFSpeechRecognizer</code> with <code>requiresOnDeviceRecognition = true</code> or local Whisper models). Audio buffers stay in host memory and never touch an external network interface.</li>\n</ul>\n<h3>2. Screen Context &amp; Frame Retention</h3>\n<ul>\n<li><strong>Cloud Architecture</strong>: High-resolution screen captures or active window pixels are uploaded to remote multimodal LLMs. Visual data containing sensitive emails, financial documents, or source code leaves the physical machine.</li>\n<li><strong>Local Architecture</strong>: Screen context is captured locally via <code>ScreenCaptureKit</code>, processed via native macOS Accessibility (<code>AXUIElement</code>) APIs or native OCR, and optionally analyzed by a local Vision-Language Model (VLM) running on loopback (<code>127.0.0.1</code>). Frame buffers are purged immediately after execution.</li>\n</ul>\n<h3>3. Network Independence &amp; Offline Capability</h3>\n<ul>\n<li><strong>Cloud Architecture</strong>: Unusable without an active internet connection. Network congestion, DNS failures, or remote API outages halt dictation and action execution.</li>\n<li><strong>Local Architecture</strong>: Fully functional offline. Speech recognition, reasoning, and system action execution run locally on Apple Silicon hardware without requiring network connectivity.</li>\n</ul>\n<h3>4. API Key &amp; Credential Storage</h3>\n<ul>\n<li><strong>Cloud Architecture</strong>: Third-party API keys or OAuth tokens may be stored in plain-text configuration files (<code>.env</code> or property lists) or managed remotely by service vendors.</li>\n<li><strong>Local Architecture</strong>: Credentials for optional external services reside exclusively in the native macOS Keychain (<code>PaceKeychainStore</code>), protected by system-level encryption and sandbox boundaries.</li>\n</ul>\n<h3>5. Auditability &amp; System Telemetry</h3>\n<ul>\n<li><strong>Cloud Architecture</strong>: User interaction metrics, spoken command frequencies, and active app names are typically captured by vendor analytics SDKs and uploaded to remote telemetry dashboards.</li>\n<li><strong>Local Architecture</strong>: Zero remote telemetry. All execution logs and API audit records stay on the local filesystem (e.g., <code>~/Library/Application Support/Pace/api-audit-log.jsonl</code>), giving the user complete visibility and control over their data history.</li>\n</ul>\n<h3>6. Action Safety &amp; Reversible Execution</h3>\n<ul>\n<li><strong>Cloud Architecture</strong>: Remote planner models dispatch action commands without granular local preflight checks or immediate user rollback mechanisms.</li>\n<li><strong>Local Architecture</strong>: High-risk actions (such as file deletion or external communications) require explicit modal approval with default-cancel safety. Reversible mutations present a floating 5-second undo banner and local session recovery.</li>\n</ul>\n<h2>Comparing Local vs. Cloud Voice Agent Architectures</h2>\n<table>\n<thead>\n<tr>\n<th style=\"text-align: left\">Feature / Dimension</th>\n<th style=\"text-align: left\">Local On-Device Architecture</th>\n<th style=\"text-align: left\">Cloud-Based AI Architecture</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style=\"text-align: left\"><strong>Audio Processing</strong></td>\n<td style=\"text-align: left\">On-device ASR (<code>SFSpeechRecognizer</code> / Whisper)</td>\n<td style=\"text-align: left\">Remote audio streaming</td>\n</tr>\n<tr>\n<td style=\"text-align: left\"><strong>Screen Data Route</strong></td>\n<td style=\"text-align: left\">Local <code>ScreenCaptureKit</code> + local VLM</td>\n<td style=\"text-align: left\">Cloud frame transmission</td>\n</tr>\n<tr>\n<td style=\"text-align: left\"><strong>Offline Functionality</strong></td>\n<td style=\"text-align: left\">Fully operational offline</td>\n<td style=\"text-align: left\">Inoperable without internet</td>\n</tr>\n<tr>\n<td style=\"text-align: left\"><strong>Response Latency</strong></td>\n<td style=\"text-align: left\">Direct local execution (no network overhead)</td>\n<td style=\"text-align: left\">Bound by network round-trip time</td>\n</tr>\n<tr>\n<td style=\"text-align: left\"><strong>Telemetry &amp; Tracking</strong></td>\n<td style=\"text-align: left\">Zero remote telemetry; local JSONL logs</td>\n<td style=\"text-align: left\">Vendor cloud analytics &amp; logging</td>\n</tr>\n<tr>\n<td style=\"text-align: left\"><strong>Credential Storage</strong></td>\n<td style=\"text-align: left\">Protected in native macOS Keychain</td>\n<td style=\"text-align: left\">Plain-text config or remote servers</td>\n</tr>\n<tr>\n<td style=\"text-align: left\"><strong>Action Confirmation</strong></td>\n<td style=\"text-align: left\">Modal approval prompts + local undo banner</td>\n<td style=\"text-align: left\">Opaque remote command dispatch</td>\n</tr>\n<tr>\n<td style=\"text-align: left\"><strong>Hardware Requirement</strong></td>\n<td style=\"text-align: left\">Apple Silicon with adequate unified RAM</td>\n<td style=\"text-align: left\">Minimal local RAM; relies on remote GPUs</td>\n</tr>\n</tbody>\n</table>\n<h2>Managing Off-Device Exceptions Responsibly</h2>\n<p>While an on-device architecture provides maximum privacy by default, certain workflows may require opting into external cloud LLMs (such as using a personal API key or connecting a developer CLI tool like <code>codex</code> or <code>claude</code>). A responsible system architecture handles off-device exceptions through clear security mechanics:</p>\n<ol>\n<li><strong>Explicit Opt-In and Transport Consent</strong>: Off-device routing is never active by default. Users must deliberately enable cloud options and grant transport consent.</li>\n<li><strong>Soak Period Safety Gates</strong>: Sensitive CLI direct-spawns enforce safety timers (such as a 24-hour soak period) to ensure background tasks do not prematurely route data off-device.</li>\n<li><strong>Unambiguous Visual Signals</strong>: Whenever an active request routes off-device, the application UI clearly signals this transition—such as tinting the active menu bar status capsule <strong>amber</strong>.</li>\n<li><strong>Local Audit Logging</strong>: Every off-device turn records the destination, timestamp, and byte payload size in a local API audit log, ensuring complete transparency.</li>\n<li><strong>Fail-Loud Error Recovery</strong>: If an off-device connection fails, the system presents clear error feedback via plain-language failure narrators rather than silently dropping context or falling back unannounced.</li>\n</ol>\n<h2>Decision Matrix: Which Architecture Fits Your Workflow?</h2>\n<h3>Choose a Local On-Device Assistant If You:</h3>\n<ul>\n<li>Handle confidential source code, trade secrets, legal client files, or healthcare data subject to compliance requirements.</li>\n<li>Require reliable voice control and desktop automation while offline or on restricted corporate networks.</li>\n<li>Want zero telemetry tracking, local Keychain credential security, and total ownership over application logs.</li>\n<li>Operate modern Apple Silicon hardware with unified RAM configured for local model execution.</li>\n</ul>\n<h3>Consider Cloud Planners As Explicit Options If You:</h3>\n<ul>\n<li>Require extreme parameter-count reasoning models for complex open-domain queries that exceed local RAM capacity.</li>\n<li>Work on hardware with limited local memory where heavy local LLMs/VLMs cannot run concurrently.</li>\n<li>Explicitly consent to remote API routing and maintain appropriate organizational API keys in your macOS Keychain.</li>\n</ul>\n<h2>Conclusion and Next Steps</h2>\n<p>Evaluating desktop voice agents requires looking beyond surface-level features to examine the underlying trust boundary. While cloud architectures offer convenience, local on-device voice agents provide uncompromised data privacy, offline resilience, and transparent action control. By applying this 6-point evaluation checklist, organizations and individual power users can select a voice assistant architecture that aligns with their security requirements.</p>\n<h2>Clear Next Action</h2>\n<p>To test an on-device voice agent that strictly adheres to this privacy and capability checklist, <a href=\"/download\">download Pace for macOS</a> or explore our <a href=\"/privacy\">privacy and trust documentation</a>.</p>"
  },
  "/screen-aware-mac-assistants-vision-planning-approval": {
    "title": "Screen-Aware Mac Assistants: Local Vision, Planning, and Approval",
    "description": "Discover how screen-aware macOS assistants use local vision, Accessibility tree inspection, bounded plan-act-observe loops, and approval gates to automate tasks safely.",
    "path": "/screen-aware-mac-assistants-vision-planning-approval",
    "intent": "technical explainer on how screen-aware Mac assistants capture local context and gate desktop actions",
    "schema": [
      "TechArticle",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/screen-aware-ai-assistant-mac",
      "/mac-ai-assistant-actions",
      "/private-voice-assistant-mac",
      "/on-device-ai-assistant-mac",
      "/privacy",
      "/download"
    ],
    "markdown": "Traditional desktop voice assistants operate in a vacuum. When a user asks a basic voice assistant to \"click the green submit button in my browser\" or \"summarize the open document,\" conventional software fails unless the application exposes explicit, pre-defined voice shortcuts. A truly *screen-aware* assistant bridges this gap by interpreting the user's active display—understanding visual layouts, window hierarchies, active controls, and text content in real time.\n\nHowever, giving an artificial intelligence agent access to live screen contents introduces dual engineering challenges: processing high-resolution visual data rapidly without streaming desktop video feeds to remote cloud servers, and ensuring that automated screen actions (such as clicking buttons or editing values) occur predictably under explicit user control. Understanding how local vision, multimodal reasoning, and action approval policies interact reveals how modern Mac assistants achieve safe, context-aware automation.\n\n## The Local Vision Pipeline: Capture, Inspection, and VLM Fallbacks\n\nA privacy-preserving screen-aware agent uses a multi-stage local perception pipeline to understand what is happening on screen without transmitting images off the user's Mac:\n\n```text\n[ScreenCaptureKit Frame Capture] ──► [AXUIElement & Native OCR Inspection]\n                                            │\n                                            ▼\n                               [Is AX/OCR sufficient?]\n                                  │               │\n                                Yes               No\n                                  │               │\n                                  ▼               ▼\n                        [Direct AX Target]  [Local VLM Fallback]\n```\n\n### 1. High-Performance Frame Capture\n\nVisual perception begins with capturing the display state. On modern macOS systems, local agents utilize Apple's `ScreenCaptureKit` framework. ScreenCaptureKit provides hardware-accelerated, multi-monitor display frame access with minimal CPU overhead, capturing the exact pixel representation of active application windows.\n\n### 2. Accessibility (AX) Tree and Native OCR Inspection\n\nPixels alone are often inefficient for precise interaction. Before sending display frames to heavy neural network models, the local agent performs lightweight structural inspection. The system queries the macOS Accessibility hierarchy via `AXUIElement` APIs, extracting control identifiers, button labels, text field values, and bounding frame coordinates. Simultaneously, native operating system Optical Character Recognition (OCR) processes visual text blocks. If a target element (such as a standard macOS button or menu item) is exposed in the AX tree, the agent targets it directly with exact coordinate confidence.\n\n### 3. Local Vision-Language Model (VLM) Fallbacks\n\nWhen interacting with custom canvas applications, web games, electron apps, or creative software where Accessibility elements are incomplete or unexposed, the agent falls back to a local Vision-Language Model (VLM)—such as `qwen/qwen3.5-4b` multimodal or specialized local UI vision models running over local loopback connections (`127.0.0.1`). The local VLM analyzes the display image alongside the user's request, emitting spatial coordinates or visual tags (e.g., `[POINT:x,y:label]`) to guide the action executor without external network transmission.\n\n## The Multi-Step Plan-Act-Observe Execution Loop\n\nAutomating realistic desktop tasks requires more than a single click; it involves a continuous feedback loop of planning, acting, and observing. A screen-aware Mac agent executes this workflow using an iterative multi-step agent loop:\n\n1. **Plan**: The local reasoner analyzes the current transcript, conversation history, and visual screen context to determine the immediate next step.\n2. **Act**: The executor dispatches the step—such as clicking a coordinate (`[CLICK:x,y]`), typing text into an active control (`[TYPE:text]`), or pressing a key chord (`[KEY:cmd+s]`). Preference is always given to Accessibility-native presses (`PaceAXTargeter`) before falling back to system mouse events (`CGEvent`).\n3. **Observe**: Once the action dispatches, the agent captures an updated screen snapshot or checks Accessibility state changes to verify the result before proceeding.\n\nTo guarantee that an agent never gets stuck in an unresolvable loop, execution is subject to strict bounds. The planner enforces a step cap (`AgentMaxSteps`, defaulting to 8 iterations). If a task cannot be completed within the step limit, or if the model emits a final `[DONE]` signal, the agent terminates execution cleanly and reports outcomes to the user.\n\nFurthermore, state verification uses lightweight Accessibility polling (\"FastPath observation\") to detect visual window changes within milliseconds, returning immediately when an element updates rather than enforcing long static delays.\n\n## Action Safety Framework: Approval Policy, Preflight, and Reversibility\n\nGranting an AI agent the ability to execute physical clicks and keypresses on a desktop computer requires rigorous safety guardrails. A screen-aware Mac assistant enforces safety through a three-tier protection layer:\n\n### 1. Action Risk Categorization\n\nNot all desktop interactions carry equal risk. Read-only navigation (such as switching tabs, scrolling a page, or reading window titles) carries minimal risk and is permitted to execute fluidly. Conversely, actions that mutate persistent state—such as deleting files, executing system commands, sending emails, or modifying calendar entries—are flagged as high-risk or external operations.\n\n### 2. Explicit User Approval Prompts\n\nWhen the user enables action confirmation preferences (`Approve Risky Actions`), any high-risk or non-undoable action halts execution before dispatch. The agent presents an explicit modal prompt detailing the target application and proposed action. The default selection on the approval dialog is set to **Cancel**, ensuring that unattended or ambiguous commands cannot execute without active human consent.\n\n### 3. Reversible Mutations and Local Undo\n\nWhen reversible mutations execute (such as creating a note, editing text, or creating a reminder), the agent records the previous state in a session mutation log and displays a floating, 5-second **Undo Banner** near the cursor. Tapping the banner or uttering \"undo that\" immediately triggers a rollback via the local action executor, restoring the system to its prior state.\n\n## Transparency and Trust Boundaries for Screen-Aware Agents\n\nBecause screen-aware agents inspect active window contents, user trust requires total transparency regarding how visual data is handled:\n\n- **Local Processing Default**: All captured display frames, OCR text buffers, and VLM visual embeddings remain strictly in local RAM and are discarded after turn completion. No screen images are uploaded to remote servers.\n- **Visual State Communication**: Active agent states (listening, reasoning, acting, speaking) are explicitly rendered in native UI elements (such as a Living Notch signal or status panel).\n- **Off-Device Disclosures**: If the user explicitly opts into an off-device reasoning model (such as a cloud API or CLI bridge to external models), the menu bar capsule turns **amber**, and every off-device request is logged to a local JSONL audit trail (`~/Library/Application Support/Pace/api-audit-log.jsonl`).\n- **Loopback Endpoint Guarding**: All local model communication interfaces bind strictly to `127.0.0.1`, preventing external devices on the local network from querying local VLM or planner interfaces.\n\n## Conclusion and Best Practices for Screen-Aware Desktop Automation\n\nScreen-aware Mac assistants represent a major evolution in desktop productivity, combining high-speed visual capture with intelligent action execution. By pairing local ScreenCaptureKit frame acquisition with Accessibility tree inspection, local VLM fallbacks, bounded plan-act-observe loops, explicit action approval gates, and transparent local audit logging, users can automate daily desktop workflows safely and privately.\n\n## Clear Next Action\n\nTo see how screen-aware local vision and action approvals operate on your desktop, visit our [screen-aware assistant guide](/screen-aware-ai-assistant-mac) or review the complete security model on our [privacy page](/privacy).",
    "html": "<p>Traditional desktop voice assistants operate in a vacuum. When a user asks a basic voice assistant to \"click the green submit button in my browser\" or \"summarize the open document,\" conventional software fails unless the application exposes explicit, pre-defined voice shortcuts. A truly <em>screen-aware</em> assistant bridges this gap by interpreting the user&#39;s active display—understanding visual layouts, window hierarchies, active controls, and text content in real time.</p>\n<p>However, giving an artificial intelligence agent access to live screen contents introduces dual engineering challenges: processing high-resolution visual data rapidly without streaming desktop video feeds to remote cloud servers, and ensuring that automated screen actions (such as clicking buttons or editing values) occur predictably under explicit user control. Understanding how local vision, multimodal reasoning, and action approval policies interact reveals how modern Mac assistants achieve safe, context-aware automation.</p>\n<h2>The Local Vision Pipeline: Capture, Inspection, and VLM Fallbacks</h2>\n<p>A privacy-preserving screen-aware agent uses a multi-stage local perception pipeline to understand what is happening on screen without transmitting images off the user&#39;s Mac:</p>\n<pre class=\"astro-code github-dark\" style=\"background-color:#24292e;color:#e1e4e8; overflow-x: auto;\" tabindex=\"0\" data-language=\"text\"><code><span class=\"line\"><span>[ScreenCaptureKit Frame Capture] ──► [AXUIElement &amp; Native OCR Inspection]</span></span>\n<span class=\"line\"><span>                                            │</span></span>\n<span class=\"line\"><span>                                            ▼</span></span>\n<span class=\"line\"><span>                               [Is AX/OCR sufficient?]</span></span>\n<span class=\"line\"><span>                                  │               │</span></span>\n<span class=\"line\"><span>                                Yes               No</span></span>\n<span class=\"line\"><span>                                  │               │</span></span>\n<span class=\"line\"><span>                                  ▼               ▼</span></span>\n<span class=\"line\"><span>                        [Direct AX Target]  [Local VLM Fallback]</span></span></code></pre>\n<h3>1. High-Performance Frame Capture</h3>\n<p>Visual perception begins with capturing the display state. On modern macOS systems, local agents utilize Apple&#39;s <code>ScreenCaptureKit</code> framework. ScreenCaptureKit provides hardware-accelerated, multi-monitor display frame access with minimal CPU overhead, capturing the exact pixel representation of active application windows.</p>\n<h3>2. Accessibility (AX) Tree and Native OCR Inspection</h3>\n<p>Pixels alone are often inefficient for precise interaction. Before sending display frames to heavy neural network models, the local agent performs lightweight structural inspection. The system queries the macOS Accessibility hierarchy via <code>AXUIElement</code> APIs, extracting control identifiers, button labels, text field values, and bounding frame coordinates. Simultaneously, native operating system Optical Character Recognition (OCR) processes visual text blocks. If a target element (such as a standard macOS button or menu item) is exposed in the AX tree, the agent targets it directly with exact coordinate confidence.</p>\n<h3>3. Local Vision-Language Model (VLM) Fallbacks</h3>\n<p>When interacting with custom canvas applications, web games, electron apps, or creative software where Accessibility elements are incomplete or unexposed, the agent falls back to a local Vision-Language Model (VLM)—such as <code>qwen/qwen3.5-4b</code> multimodal or specialized local UI vision models running over local loopback connections (<code>127.0.0.1</code>). The local VLM analyzes the display image alongside the user&#39;s request, emitting spatial coordinates or visual tags (e.g., <code>[POINT:x,y:label]</code>) to guide the action executor without external network transmission.</p>\n<h2>The Multi-Step Plan-Act-Observe Execution Loop</h2>\n<p>Automating realistic desktop tasks requires more than a single click; it involves a continuous feedback loop of planning, acting, and observing. A screen-aware Mac agent executes this workflow using an iterative multi-step agent loop:</p>\n<ol>\n<li><strong>Plan</strong>: The local reasoner analyzes the current transcript, conversation history, and visual screen context to determine the immediate next step.</li>\n<li><strong>Act</strong>: The executor dispatches the step—such as clicking a coordinate (<code>[CLICK:x,y]</code>), typing text into an active control (<code>[TYPE:text]</code>), or pressing a key chord (<code>[KEY:cmd+s]</code>). Preference is always given to Accessibility-native presses (<code>PaceAXTargeter</code>) before falling back to system mouse events (<code>CGEvent</code>).</li>\n<li><strong>Observe</strong>: Once the action dispatches, the agent captures an updated screen snapshot or checks Accessibility state changes to verify the result before proceeding.</li>\n</ol>\n<p>To guarantee that an agent never gets stuck in an unresolvable loop, execution is subject to strict bounds. The planner enforces a step cap (<code>AgentMaxSteps</code>, defaulting to 8 iterations). If a task cannot be completed within the step limit, or if the model emits a final <code>[DONE]</code> signal, the agent terminates execution cleanly and reports outcomes to the user.</p>\n<p>Furthermore, state verification uses lightweight Accessibility polling (\"FastPath observation\") to detect visual window changes within milliseconds, returning immediately when an element updates rather than enforcing long static delays.</p>\n<h2>Action Safety Framework: Approval Policy, Preflight, and Reversibility</h2>\n<p>Granting an AI agent the ability to execute physical clicks and keypresses on a desktop computer requires rigorous safety guardrails. A screen-aware Mac assistant enforces safety through a three-tier protection layer:</p>\n<h3>1. Action Risk Categorization</h3>\n<p>Not all desktop interactions carry equal risk. Read-only navigation (such as switching tabs, scrolling a page, or reading window titles) carries minimal risk and is permitted to execute fluidly. Conversely, actions that mutate persistent state—such as deleting files, executing system commands, sending emails, or modifying calendar entries—are flagged as high-risk or external operations.</p>\n<h3>2. Explicit User Approval Prompts</h3>\n<p>When the user enables action confirmation preferences (<code>Approve Risky Actions</code>), any high-risk or non-undoable action halts execution before dispatch. The agent presents an explicit modal prompt detailing the target application and proposed action. The default selection on the approval dialog is set to <strong>Cancel</strong>, ensuring that unattended or ambiguous commands cannot execute without active human consent.</p>\n<h3>3. Reversible Mutations and Local Undo</h3>\n<p>When reversible mutations execute (such as creating a note, editing text, or creating a reminder), the agent records the previous state in a session mutation log and displays a floating, 5-second <strong>Undo Banner</strong> near the cursor. Tapping the banner or uttering \"undo that\" immediately triggers a rollback via the local action executor, restoring the system to its prior state.</p>\n<h2>Transparency and Trust Boundaries for Screen-Aware Agents</h2>\n<p>Because screen-aware agents inspect active window contents, user trust requires total transparency regarding how visual data is handled:</p>\n<ul>\n<li><strong>Local Processing Default</strong>: All captured display frames, OCR text buffers, and VLM visual embeddings remain strictly in local RAM and are discarded after turn completion. No screen images are uploaded to remote servers.</li>\n<li><strong>Visual State Communication</strong>: Active agent states (listening, reasoning, acting, speaking) are explicitly rendered in native UI elements (such as a Living Notch signal or status panel).</li>\n<li><strong>Off-Device Disclosures</strong>: If the user explicitly opts into an off-device reasoning model (such as a cloud API or CLI bridge to external models), the menu bar capsule turns <strong>amber</strong>, and every off-device request is logged to a local JSONL audit trail (<code>~/Library/Application Support/Pace/api-audit-log.jsonl</code>).</li>\n<li><strong>Loopback Endpoint Guarding</strong>: All local model communication interfaces bind strictly to <code>127.0.0.1</code>, preventing external devices on the local network from querying local VLM or planner interfaces.</li>\n</ul>\n<h2>Conclusion and Best Practices for Screen-Aware Desktop Automation</h2>\n<p>Screen-aware Mac assistants represent a major evolution in desktop productivity, combining high-speed visual capture with intelligent action execution. By pairing local ScreenCaptureKit frame acquisition with Accessibility tree inspection, local VLM fallbacks, bounded plan-act-observe loops, explicit action approval gates, and transparent local audit logging, users can automate daily desktop workflows safely and privately.</p>\n<h2>Clear Next Action</h2>\n<p>To see how screen-aware local vision and action approvals operate on your desktop, visit our <a href=\"/screen-aware-ai-assistant-mac\">screen-aware assistant guide</a> or review the complete security model on our <a href=\"/privacy\">privacy page</a>.</p>"
  },
};

